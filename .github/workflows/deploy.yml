# name: Deploy to VPS
# on:
#   push:
#     branches:
#       - master
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#     - name: Checkout source code
#       uses: actions/checkout@v2

#     - name: Install Dependencies
#       run: |
#         # cd /root/todo_app/my-app
#         npm install
#     - name: Build and Package
#       run: |
#         # cd /root/todo_app/my-app
#         npm run build
#         tar -zcvf build.tar.gz build
#     - name: Deploy to VPS
#       uses: appleboy/ssh-action@master
#       with:
#         host: 45.146.166.151
#         username: latypov12
#         password: ${{ secrets.SSH_PASSWORD }}
#         script: |
#           mkdir -p /var/www/your-react-app
#           cd /var/www/your-react-app
#           rm -rf *
#           tar -zxvf $HOME/build.tar.gz

name: Deploy React App

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install Node.js and NPM
      uses: actions/setup-node@v1
      with:
        node-version: '18.x'

    - name: Install dependencies
      run: npm install

    - name: Build and package app
      run: npm run build

    - name: Deploy to VPS
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USERNAME }}
        password: ${{ secrets.SSH_PASSWORD }}
        script: |
          cd /var/www/react-app/
          rm -rf * 
          mkdir build
          scp -r -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -P 22 ./build/* ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }}:/var/www/react-app/